# GitHub の参照・操作

- タスクに必要な GitHub の情報は、コマンドの指定をユーザーに求めず GitHub CLI で取得する。
- Bash で `source "$(git rev-parse --show-toplevel)/scripts/gh-wrappers.bash"` を実行してから利用する。非対話シェルでは `.bashrc` の自動読み込みに依存しない。
- このリポジトリの参照・許可された書き込みには `ghp`（保存済み認証）、他リポジトリの参照には `ghw`（読み取り用認証）を使う。`gh` の直接実行による認証の混在を避ける。
- 対象はユーザーの指定・URL・Git remote から特定し、必要に応じて `-R OWNER/REPO` を指定する。リポジトリ名を推測しない。
- `ghw` の認証・権限エラー時に `ghp` や別の認証へ自動で切り替えない。エラーを報告し、権限や認証設定を勝手に変更しない。
- トークンや認証ファイルの内容を閲覧・表示しない。認証情報の読み込みはラッパーと `gh` に任せる。
- 導入・更新手順が必要な場合は `docs/github-cli.md` を参照する。

# 使用言語

- GitHub 上の Issue / PR / コメントや、ユーザーとの会話では日本語を使用する。
